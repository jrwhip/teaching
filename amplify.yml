version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - node --version
        - npm ci --ignore-scripts
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/teaching/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
