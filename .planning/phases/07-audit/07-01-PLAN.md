---
phase: 07-audit
type: execute
---

<objective>
Visual and code audit of all math components for dark mode, responsive layout, and token usage.

Purpose: Verify that no hardcoded colors, inline styles, or removed artifacts leaked through across the 7 math components, math menu, and results dashboards.
Output: Clean audit confirming all components meet design system standards.
</objective>

<context>
@.planning/BRIEF.md
@src/styles/_tokens.scss
@src/styles/_tokens-dark.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan SCSS for hardcoded colors</name>
  <files>src/app/practice/math/**/*.scss</files>
  <action>Grep all math component SCSS files for hardcoded color values (#hex, rgb(), named colors like black/white/gray). Only legitimate exception: #fff for text on var(--color-primary) background buttons. Flag anything else.</action>
  <verify>Only legitimate #fff instances found</verify>
  <done>No unauthorized hardcoded colors in any math component SCSS</done>
</task>

<task type="auto">
  <name>Task 2: Scan HTML for inline color styles</name>
  <files>src/app/practice/math/**/*.html</files>
  <action>Grep all math component HTML templates for [style.color], [style.background], style="*color*" inline declarations. These should all use CSS classes instead.</action>
  <verify>No inline color styles found</verify>
  <done>No inline color declarations in any math component HTML</done>
</task>

<task type="auto">
  <name>Task 3: Verify removed artifacts</name>
  <files>src/app/practice/math/**/*.ts</files>
  <action>Grep for colorTheme, colorOptions, setColor across all math TS files. Should return zero results — all per-component color pickers were removed. Also verify canvas components reference canvas-theme.util and global .frac import has no component-level duplicates.</action>
  <verify>Zero results for colorTheme/colorOptions/setColor, canvas-theme.util referenced, no .frac duplicates</verify>
  <done>All removed artifacts confirmed gone, all integrations verified</done>
</task>

</tasks>

<verification>
- [ ] No hardcoded colors (except legitimate #fff on primary buttons)
- [ ] No inline color styles in HTML
- [ ] No colorTheme/colorOptions/setColor artifacts
- [ ] Canvas components use theme utility
- [ ] Global .frac import present, no duplicates
</verification>

<success_criteria>
- Clean audit across all 7 math components + menu + dashboards
- All colors sourced from design tokens
- Dark mode compatible throughout
- No leftover artifacts from pre-restyle code
</success_criteria>
