---
phase: 04-main-quiz
type: execute
---

<objective>
Full restyle of the main quiz component — the most complex math component.

Purpose: Remove the quiz's own navbar and color picker, restructure the layout with an accordion sidebar for problem type navigation, apply design tokens throughout, and wire MathResultsService.
Output: Quiz renders with accordion sidebar, stats bar, problem card, hint, and answer history — all token-based, responsive, dark-mode compatible.
</objective>

<context>
@.planning/BRIEF.md
@src/app/practice/math/quiz/quiz.component.ts
@src/app/practice/math/quiz/quiz.component.html
@src/app/practice/math/quiz/quiz.component.scss
@src/app/practice/math/quiz/problem.model.ts
@src/app/practice/math/shared/math-results.service.ts
@src/app/practice/math/shared/problem-taxonomy.ts
@src/styles/_tokens.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove quiz navbar and color picker</name>
  <files>src/app/practice/math/quiz/quiz.component.ts, src/app/practice/math/quiz/quiz.component.html, src/app/practice/math/quiz/quiz.component.scss</files>
  <action>Delete nav.quiz-navbar and all associated SCSS. Remove colorTheme signal, colorOptions array, setColor() method, [style.background-color] binding from wrapper. Remove updateStorage() localStorage logic and getNextSunday() helper.</action>
  <verify>pnpm build passes</verify>
  <done>No quiz-navbar, no colorTheme/colorOptions/setColor, no updateStorage/getNextSunday</done>
</task>

<task type="auto">
  <name>Task 2: Restructure layout with accordion sidebar</name>
  <files>src/app/practice/math/quiz/quiz.component.html, src/app/practice/math/quiz/quiz.component.scss</files>
  <action>New template structure: .section > .container > .quiz-layout (flex row). Sidebar: .card-flat vertical nav with collapsible category groups (accordion pattern). Each category header clickable, expands to show type buttons. Active type has var(--color-primary) left border accent. Main area: stats bar (.card-flat with correct/incorrect counts + font sizer), problem card (.card), hint (.alert-warning-style with rgba and border-left), answer history (.card-flat with success/danger text). Responsive: at mobile width (<768px) sidebar collapses to horizontal scrollable pill bar.</action>
  <verify>pnpm build passes</verify>
  <done>Accordion sidebar navigates problem types, stats bar shows counts, responsive at mobile</done>
</task>

<task type="auto">
  <name>Task 3: Wire MathResultsService</name>
  <files>src/app/practice/math/quiz/quiz.component.ts</files>
  <action>Inject MathResultsService. Call startNewSession() on init. Call recordAttempt() in checkAnswer() with problem type from activeProblemType() and taxonomy lookup via getTaxonomy().</action>
  <verify>pnpm build passes</verify>
  <done>Results tracked for all 48+ quiz problem types</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Accordion sidebar works (expand/collapse categories, select types)
- [ ] All 48+ problem types generate correctly
- [ ] Dark mode renders all elements correctly
- [ ] Mobile layout collapses sidebar to pill bar
- [ ] Hint and answer history still work
</verification>

<success_criteria>
- Complete structural restyle from tabbed navbar to accordion sidebar
- All colors from design tokens
- MathResultsService wired for all problem types
- Responsive and dark-mode compatible
</success_criteria>
