---
phase: 06-teacher-parent-dashboards
type: execute
---

<objective>
Build role-guarded results dashboards for teachers and parents with drill-down to student attempt history.

Purpose: Teachers need to see their classroom students' math results. Parents need to see their linked students' results. Both need drill-down to individual attempt history.
Output: teacher-results and parent-results components, role-guarded routes, dashboard nav links.
</objective>

<context>
@.planning/BRIEF.md
@src/app/core/auth/auth.service.ts
@src/app/core/auth/auth.guard.ts
@src/app/core/data/data.service.ts
@src/app/practice/practice.routes.ts
@src/app/dashboard/teacher/teacher-home.component.ts
@src/app/dashboard/parent/parent-home.component.ts
@amplify/data/resource.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teacher results component</name>
  <files>src/app/practice/math/results/teacher-results.component.ts, src/app/practice/math/results/teacher-results.component.html, src/app/practice/math/results/teacher-results.component.scss</files>
  <action>Fetch classrooms by teacherId, enrollments by classroomId, student profiles, performance counters. Signals: loading, students (StudentSummary[]), selectedStudent, selectedAttempts, detailLoading. Roster table with Student, Problems, Accuracy, Best Streak, Last Active columns. Clickable rows drill into student detail with 20 most recent ProblemAttempts (DESC). Loading and empty states. Token-based SCSS with .results-table, .clickable-row hover, .row-correct/.row-incorrect coloring.</action>
  <verify>pnpm build passes</verify>
  <done>Teacher dashboard shows student roster with drill-down to attempt history</done>
</task>

<task type="auto">
  <name>Task 2: Create parent results component</name>
  <files>src/app/practice/math/results/parent-results.component.ts, src/app/practice/math/results/parent-results.component.html, src/app/practice/math/results/parent-results.component.scss</files>
  <action>Similar to teacher but uses listParentStudentLinkByParentId instead of classrooms. Card-based student view (not table) with per-child stats. Same drill-down to attempt history. Signals: loading, students, selectedStudent, selectedAttempts, detailLoading. Token-based SCSS with .student-cards grid, .student-card with hover shadow.</action>
  <verify>pnpm build passes</verify>
  <done>Parent dashboard shows linked students with drill-down</done>
</task>

<task type="auto">
  <name>Task 3: Add routes and dashboard navigation</name>
  <files>src/app/practice/practice.routes.ts, src/app/dashboard/teacher/teacher-home.component.ts, src/app/dashboard/parent/parent-home.component.ts</files>
  <action>Add math/results/teacher route with canActivate: [roleGuard('TEACHER')]. Add math/results/parent route with canActivate: [roleGuard('PARENT')]. Add "Student Results" link to teacher dashboard home. Add "View Progress" link to parent dashboard home.</action>
  <verify>pnpm build passes</verify>
  <done>Routes role-guarded, navigation links present in dashboards</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Teacher route guarded by TEACHER role
- [ ] Parent route guarded by PARENT role
- [ ] Teacher dashboard home has results link
- [ ] Parent dashboard home has progress link
- [ ] Both dashboards have loading and empty states
</verification>

<success_criteria>
- Teacher sees classroom students' results with drill-down
- Parent sees linked students' results with drill-down
- Routes role-guarded (students cannot access)
- Navigation integrated into dashboard home pages
</success_criteria>
