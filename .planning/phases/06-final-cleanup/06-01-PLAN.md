---
phase: 06-final-cleanup
plan: 01
type: execute
---

<objective>
Fix the 3 remaining code guide violations: 2 `for` loops in smart quiz code and 1 `console.error` in math results service.

Purpose: Close out v1.2 code quality compliance. Zero violations.
Output: Clean code, zero grep hits for banned patterns.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@ANGULAR-GUIDE.md
@src/app/practice/math/shared/weakness-analyzer.ts
@src/app/practice/math/shared/error-analyzer.ts
@src/app/practice/math/shared/math-results.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace for loops with functional methods</name>
  <files>
    src/app/practice/math/shared/weakness-analyzer.ts,
    src/app/practice/math/shared/error-analyzer.ts
  </files>
  <action>
    Two `for` loop violations:

    1. `weakness-analyzer.ts:70` — `weightedRandomPick()` uses `for (const w of weights)` with early return for weighted random selection. Replace with `Array.prototype.find()` using a running sum via `reduce()`, or use `findIndex()` with accumulation. The key is preserving the weighted random pick semantics: subtract each weight from a random value, return first item where remainder <= 0.

    2. `error-analyzer.ts:258` — `for (const place of otherPlaces)` with `continue` and early return. This iterates place values checking if the student rounded to the wrong place. Replace with `Array.prototype.find()` — find the first place where the student's answer matches rounding to that place.

    ANGULAR-GUIDE.md says: zero for loops, all functional methods, no exceptions. These are both iterable-with-early-exit patterns that map to `find()`.
  </action>
  <verify>
    grep -rn "^\s*for\s*(" src/app --include="*.ts" returns zero results.
    pnpm build passes.
  </verify>
  <done>Zero for loops in the codebase. Both functions use functional array methods.</done>
</task>

<task type="auto">
  <name>Task 2: Remove console.error from production code</name>
  <files>
    src/app/practice/math/shared/math-results.service.ts
  </files>
  <action>
    Line 205 has `console.error('Failed to record attempt:', err)` inside a `catchError` handler. This is in a fire-and-forget `subscribe()` for recording practice attempts.

    Remove the `console.error`. The `catchError` already returns `EMPTY` which swallows the error silently — that's the correct behavior for a fire-and-forget attempt recording (the student shouldn't see an error if their practice result fails to save). The console.error adds nothing in production.

    If the catchError handler becomes empty after removing console.error, just keep `catchError(() => EMPTY)`.
  </action>
  <verify>
    grep -rn "console\.\(log\|warn\|error\|debug\)" src/app --include="*.ts" returns zero results.
    pnpm build passes.
  </verify>
  <done>Zero console statements in production code. catchError still swallows gracefully.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds
- [ ] `grep -rn "^\s*for\s*(" src/app --include="*.ts"` returns zero results
- [ ] `grep -rn "console\.\(log\|warn\|error\|debug\)" src/app --include="*.ts"` returns zero results
</verification>

<success_criteria>
- Zero for loops
- Zero console statements
- Build passes
- All v1.2 code quality criteria met
</success_criteria>

<output>
After completion, create `.planning/phases/06-final-cleanup/06-01-SUMMARY.md`
</output>
