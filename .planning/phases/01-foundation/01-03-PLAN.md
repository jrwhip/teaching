---
phase: 01-foundation
type: execute
---

<objective>
Canvas theme utility for reading CSS custom properties into canvas 2D rendering contexts.

Purpose: Canvas elements can't use CSS variables directly. This utility bridges the gap so canvas components (number-line, inequality, coordinate-grid, area-perimeter) render with theme-appropriate colors and re-render on theme change.
Output: canvas-theme.util.ts that reads computed styles from documentElement.
</objective>

<context>
@.planning/BRIEF.md
@src/styles/_tokens.scss
@src/styles/_tokens-dark.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canvas theme utility</name>
  <files>src/app/practice/math/shared/canvas-theme.util.ts</files>
  <action>Create getCanvasTheme() function that reads CSS custom property values from document.documentElement computed style. Return object with lineColor, textColor, background, gridLines, accentColor, successColor, dangerColor. Canvas components call this on render and re-render when theme signal changes.</action>
  <verify>pnpm build passes, utility returns correct values in light and dark mode</verify>
  <done>Utility compiles, returns theme-appropriate colors for canvas rendering</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] getCanvasTheme() returns correct light mode values
- [ ] getCanvasTheme() returns correct dark mode values
</verification>

<success_criteria>
- canvas-theme.util.ts exists and compiles
- Returns CSS custom property values for canvas rendering
- Covers all color needs for the 4 canvas components
</success_criteria>
