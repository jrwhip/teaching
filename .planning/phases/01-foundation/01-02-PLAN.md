---
phase: 01-foundation
type: execute
---

<objective>
Extend ThemeService with accent background color management.

Purpose: Replace per-component color pickers (quiz, quiz2, quiz3 each had their own) with a single app-wide accent color system managed by ThemeService and accessible from the navbar.
Output: ThemeService.accentBg signal, ACCENT_COLORS palette, navbar color picker, --bg-accent CSS variable.
</objective>

<context>
@.planning/BRIEF.md
@src/app/core/theme.service.ts
@src/app/shared/layout/navbar.component.ts
@src/styles/_tokens.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ThemeService with accentBg signal</name>
  <files>src/app/core/theme.service.ts</files>
  <action>Add accentBg signal persisted to localStorage. Add ACCENT_COLORS constant (16 pastel colors from existing quiz component). Apply as --bg-accent CSS variable on document.documentElement. When dark mode active, effectiveAccent returns empty string (pastels on dark backgrounds look bad).</action>
  <verify>pnpm build passes</verify>
  <done>accentBg signal works, persists to localStorage, respects dark mode</done>
</task>

<task type="auto">
  <name>Task 2: Add color picker to navbar</name>
  <files>src/app/shared/layout/navbar.component.ts</files>
  <action>Add accent color picker UI accessible from the navbar. Display color swatches from ACCENT_COLORS. Clicking a swatch calls ThemeService to set the accent. Include a "reset" option to clear the accent.</action>
  <verify>pnpm build passes, color picker visible in navbar</verify>
  <done>Color picker renders, accent persists across reload, works with dark/light toggle</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Accent colors toggle from navbar
- [ ] Accent persists across page reload
- [ ] Dark mode suppresses accent (no pastel on dark)
</verification>

<success_criteria>
- ThemeService manages accent colors app-wide
- Single color picker in navbar replaces all per-component pickers
- Dark mode interaction handled correctly
</success_criteria>
