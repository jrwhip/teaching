---
phase: 01-foundation
type: execute
---

<objective>
Global fraction CSS styles shared across quiz components.

Purpose: Remove duplicated .frac CSS from quiz, quiz2, quiz3 component SCSS files into a single global stylesheet.
Output: _math-fractions.scss imported in styles.scss, no duplicate .frac blocks in components.
</objective>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@src/styles.scss
@src/app/practice/math/quiz/quiz.component.scss
@src/app/practice/math/quiz2/quiz2.component.scss
@src/app/practice/math/quiz3/quiz3.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global fraction stylesheet</name>
  <files>src/styles/_math-fractions.scss</files>
  <action>Create _math-fractions.scss with the .frac CSS (sup/sub block layout, border-bottom separator). This is the canonical fraction rendering style used by all quiz components.</action>
  <verify>File exists, contains .frac styles</verify>
  <done>.frac class defined with sup/sub block layout</done>
</task>

<task type="auto">
  <name>Task 2: Import in global styles</name>
  <files>src/styles.scss</files>
  <action>Import _math-fractions.scss in styles.scss alongside other partial imports.</action>
  <verify>pnpm build passes</verify>
  <done>Global import present, no build errors</done>
</task>

<task type="auto">
  <name>Task 3: Remove duplicate .frac blocks from components</name>
  <files>src/app/practice/math/quiz/quiz.component.scss, src/app/practice/math/quiz2/quiz2.component.scss, src/app/practice/math/quiz3/quiz3.component.scss</files>
  <action>Remove all :host ::ng-deep .frac blocks from quiz (1 copy), quiz2 (2 copies), quiz3 (2 copies) component SCSS files. The global stylesheet now handles these.</action>
  <verify>pnpm build passes, fractions still render in quiz/quiz2/quiz3</verify>
  <done>Zero .frac definitions in component SCSS, fractions render correctly</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] No .frac definitions in component SCSS files
- [ ] Fractions render correctly in quiz, quiz2, quiz3
</verification>

<success_criteria>
- Global _math-fractions.scss exists and is imported
- No duplicate .frac CSS in any component
- Build passes clean
</success_criteria>
