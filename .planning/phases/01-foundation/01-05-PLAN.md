---
phase: 01-foundation
type: execute
---

<objective>
Problem type taxonomy mapping internal problem type keys to database metadata.

Purpose: The quiz components use various internal string keys for problem types. The taxonomy provides a consistent mapping to display labels, categories, and problemType strings for ProblemAttempt records in DynamoDB.
Output: problem-taxonomy.ts with getTaxonomy() function and type-indexed arrays for quiz2 and quiz3.
</objective>

<context>
@.planning/BRIEF.md
@src/app/practice/math/quiz/problem.model.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create problem taxonomy</name>
  <files>src/app/practice/math/shared/problem-taxonomy.ts</files>
  <action>Create taxonomy mapping all problem types to { displayLabel, category, problemType }. Cover all 48+ quiz types (from MENU_CATEGORIES in problem.model.ts), quiz2's 20 grid types (QUIZ2_INDEX_TYPES array), quiz3's 10 grid types (QUIZ3_INDEX_TYPES array), and the 4 canvas component types (number-line, inequality, coordinate-grid, area-perimeter). Export getTaxonomy(key) function and index arrays.</action>
  <verify>pnpm build passes</verify>
  <done>All ProblemType values have taxonomy entries, getTaxonomy returns correct metadata</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Every problem type from problem.model.ts has a taxonomy entry
- [ ] QUIZ2_INDEX_TYPES and QUIZ3_INDEX_TYPES arrays cover all grid positions
- [ ] Canvas component types included
</verification>

<success_criteria>
- Complete taxonomy covering 80+ problem types
- getTaxonomy() returns display label, category, and DB problemType for any key
- Index arrays for position-based quiz2/quiz3 lookups
</success_criteria>
