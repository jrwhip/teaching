---
phase: 01-foundation
type: execute
---

<objective>
MathResultsService for tracking problem attempts to DynamoDB with localStorage fallback.

Purpose: Replace localStorage-only score tracking with persistent DynamoDB records via Amplify's ProblemAttempt and PerformanceCounter models. Service handles both authenticated (DynamoDB) and unauthenticated (graceful empty state) flows.
Output: Injectable service with session management, attempt recording, and performance counter upsert.
</objective>

<context>
@.planning/BRIEF.md
@src/app/core/auth/auth.service.ts
@src/app/core/data/data.service.ts
@amplify/data/resource.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MathResultsService</name>
  <files>src/app/practice/math/shared/math-results.service.ts</files>
  <action>Injectable service with signals: sessionId, sessionCorrect, sessionIncorrect. Methods: startNewSession() generates UUID and resets counters. recordAttempt(params) creates ProblemAttempt via DataService when authenticated, sets readAccess from student's UserProfile, upserts PerformanceCounter (fetch by studentId + problemType, create or update). Falls back gracefully when not authenticated.</action>
  <verify>pnpm build passes</verify>
  <done>Service compiles, can be injected, creates ProblemAttempt records when authenticated</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Service injectable in any math component
- [ ] startNewSession() generates UUID, resets counters
- [ ] recordAttempt() creates ProblemAttempt and upserts PerformanceCounter
</verification>

<success_criteria>
- MathResultsService exists with session and attempt management
- DynamoDB integration via DataService when authenticated
- Graceful fallback when not authenticated
</success_criteria>
