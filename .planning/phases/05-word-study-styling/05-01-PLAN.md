---
phase: 05-word-study-styling
plan: 01
type: execute
---

<objective>
Restyle all 3 Word Study components to match the app's design system. The SCSS files are empty — the section menu, word group buttons, and word list render as unstyled raw text. Make them look like the rest of the app.

Purpose: Word Study is unusable for K-6 students in its current state. It needs the same visual quality as the math components.
Output: Fully styled Word Study with design-system-compliant section menu, word group buttons, and word card grid. Works in both light and dark mode.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
@~/.claude/skills/kitt-create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@src/styles/_tokens.scss
@src/styles/_tokens-dark.scss
@src/styles/_buttons.scss
@src/styles/_cards.scss
@src/styles/_pills.scss
@src/styles/_layout.scss
@src/app/practice/math/smart-quiz/smart-quiz.component.scss
@src/app/practice/math/smart-quiz/smart-quiz.component.html
@src/app/next-step-word-study/next-step-word-study.component.ts
@src/app/next-step-word-study/next-step-word-study.component.html
@src/app/next-step-word-study/word-study-menu/word-study-menu.component.html
@src/app/next-step-word-study/word-list/word-list.component.html
@src/app/next-step-word-study/word-study-menu/word-study-menu.component.scss
@src/app/next-step-word-study/next-step-word-study.component.scss
@src/app/next-step-word-study/word-list/word-list.component.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle word-study-menu section navigation</name>
  <files>
    src/app/next-step-word-study/word-study-menu/word-study-menu.component.scss
  </files>
  <action>
    The section menu template uses BEM classes: `.section-menu`, `.section-menu__button`, `.section-menu__button--active`, `.section-menu__button--nowrap`. The SCSS file is completely empty.

    Write styles that match the app's design system:

    - `.section-menu`: flexbox, wrap, gap using `var(--sp)` spacing, padding
    - `.section-menu__button`: Use the same visual pattern as `btn-success-soft` from _buttons.scss — `background: rgba(var(--color-success-rgb), .1)`, `color: var(--color-success)`, rounded with `var(--radius)`, padding `.4rem .85rem`, `font-size: var(--text-sm)`, `font-weight: 500`, `cursor: pointer`, `text-decoration: none`, `transition: all var(--ease)`, `white-space: nowrap`
    - `.section-menu__button:hover`: `background: var(--color-success)`, `color: #fff`
    - `.section-menu__button--active`: `background: var(--color-primary)`, `color: #fff` (solid, not soft — shows which section is selected)
    - `.section-menu__button--nowrap`: `white-space: nowrap` (already in template for long labels)

    Use ONLY design system tokens. Zero hardcoded colors. This must work in dark mode automatically because all tokens resolve differently in `[data-theme="dark"]`.
  </action>
  <verify>
    pnpm build passes.
    Visually: navigate to /practice/word-study — 12 section buttons render as styled pills with spacing, wrap on small screens, active section highlighted.
  </verify>
  <done>Section menu is styled, matches design system, works in light and dark mode.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle word group buttons and parent component layout</name>
  <files>
    src/app/next-step-word-study/next-step-word-study.component.scss,
    src/app/next-step-word-study/next-step-word-study.component.html
  </files>
  <action>
    The parent template has a `.link-list` div containing word group buttons (a, i, o, e, u) plus Clear and Randomize. Currently all render as a single run-on text line "aioeuClearRandomize".

    Wrap the component in the standard `.section > .container` pattern (same as smart-quiz). Update the template:
    - Add `<div class="section"><div class="container">` wrapper around all content
    - The `.link-list` div stays but needs SCSS

    Write SCSS for `next-step-word-study.component.scss`:

    - `.link-list`: `display: flex`, `flex-wrap: wrap`, `gap: 0.5rem`, `margin-bottom: 1rem`
    - `.link-list a[role="button"]`: Style as `btn-primary-soft` pattern — `display: inline-flex`, `align-items: center`, `padding: .45rem .9rem`, `background: rgba(var(--color-primary-rgb), .1)`, `color: var(--color-primary)`, `border-radius: var(--radius)`, `font-size: var(--text-sm)`, `font-weight: 500`, `cursor: pointer`, `transition: all var(--ease)`, `text-decoration: none`
    - `.link-list a[role="button"]:hover`: `background: var(--color-primary)`, `color: #fff`
    - For Clear and Randomize, they're just more `<a role="button">` tags in the same list. They'll get the same styling which is fine — they're action buttons in the same row.

    Zero hardcoded colors. All tokens.
  </action>
  <verify>
    pnpm build passes.
    Visually: navigate to /practice/word-study/mixed-short — word group buttons (a, i, o, e, u, Clear, Randomize) render as styled buttons with spacing.
  </verify>
  <done>Word group buttons and actions are styled, layout uses section/container pattern, works in light and dark mode.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle word list as card grid</name>
  <files>
    src/app/next-step-word-study/word-list/word-list.component.scss
  </files>
  <action>
    The word list template renders words in a `.container` div with `.item` class on each `<a>` element. Currently only has an `a:hover` rule. Words display as a plain vertical text list.

    Write SCSS to display words as a responsive card grid:

    - `.container`: `display: grid`, `grid-template-columns: repeat(auto-fill, minmax(100px, 1fr))`, `gap: 0.75rem`, `margin-top: 1rem`
    - `.item`: `display: flex`, `align-items: center`, `justify-content: center`, `background: var(--bg-surface)`, `border-radius: var(--radius-lg)`, `padding: 1rem 0.5rem`, `text-align: center`, `box-shadow: var(--shadow-sm)`, `transition: all var(--ease)`, `cursor: default`, `text-decoration: none`, `color: var(--text-heading)`, `font-size: var(--text-lg)`, `font-weight: 500`
    - `.item:hover`: `box-shadow: var(--shadow)`, `transform: translateY(-2px)`, `background: var(--color-primary)`, `color: #fff`
    - `.item p`: `margin: 0` (reset default paragraph margin)

    The grid auto-fills, so it's responsive without media queries. Words appear as individual cards that a kid can point at and read. Hover gives visual feedback. All tokens, works in dark mode.
  </action>
  <verify>
    pnpm build passes.
    Visually: click a word group — words appear in a responsive grid of styled cards, not a vertical text list.
  </verify>
  <done>Word list renders as responsive card grid with design system styling, works in light and dark mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Word Study restyling — section menu, word group buttons, and word card grid all using the design system.</what-built>
  <how-to-verify>
    1. Run: pnpm start (if not already running)
    2. Visit: http://localhost:4200/practice/word-study
    3. Verify: 12 section buttons render as styled pills with spacing, wrap properly
    4. Click: "1: Mixed Short" — verify URL changes to /practice/word-study/mixed-short
    5. Verify: Word group buttons (a, i, o, e, u) and Clear/Randomize appear as styled buttons
    6. Click: "a" button — verify words appear in a card grid
    7. Click: "i" button — verify words accumulate (both a and i words shown, shuffled)
    8. Click: "Clear" — verify word list empties
    9. Click: "Randomize" — verify order changes
    10. Toggle dark mode — verify all Word Study elements respect dark theme
    11. Check mobile viewport (resize browser narrow) — verify grid and buttons wrap properly
  </how-to-verify>
  <resume-signal>Type "approved" if Word Study looks and works correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] Section menu renders with styled buttons and active state
- [ ] Word group buttons render with spacing, not raw text
- [ ] Word list renders as responsive card grid
- [ ] Dark mode works on all Word Study elements
- [ ] Human verification approved
</verification>

<success_criteria>
- All 3 SCSS files have proper design-system-compliant styles
- Zero hardcoded colors — all tokens
- Word Study visually matches the quality of math components
- Light and dark mode both work
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-word-study-styling/05-01-SUMMARY.md`
</output>
