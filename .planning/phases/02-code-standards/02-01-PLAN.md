---
phase: 02-code-standards
plan: 01
type: execute
---

<objective>
Remove all explicit `standalone: true` from decorators (Angular 20+ default), remove all `CommonModule` imports (use native control flow), and add `ChangeDetectionStrategy.OnPush` to every `@Component` decorator in the project.

Purpose: These are mechanical violations of ANGULAR-GUIDE.md that affect every component. Fix them first so subsequent plans work on a clean baseline.
Output: Every component and pipe decorator in the project is compliant with Angular 21 decorator standards.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@ANGULAR-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove standalone:true and CommonModule from all decorators</name>
  <files>
    src/app/shared/components/streak-display.component.ts,
    src/app/shared/components/stat-card.component.ts,
    src/app/dashboard/dashboard-redirect.component.ts,
    src/app/practice/math/inequality/inequality.component.ts,
    src/app/practice/math/coordinate-grid/coordinate-grid.component.ts,
    src/app/practice/math/results/teacher-results.component.ts,
    src/app/practice/math/results/parent-results.component.ts,
    src/app/nth-plain-text.pipe.ts,
    src/app/math/math.component.ts,
    src/app/next-step-word-study/next-step-word-study.component.ts
  </files>
  <action>
    Remove `standalone: true` from every @Component and @Pipe decorator — it is the default in Angular 20+.

    Remove `CommonModule` from the `imports` array of:
    - src/app/math/math.component.ts
    - src/app/next-step-word-study/next-step-word-study.component.ts

    Do NOT remove the import statement if other symbols from @angular/common are still used (there shouldn't be any, but check). The templates should already be using native control flow (@if, @for) — if they're using *ngIf or *ngFor, convert them to native control flow syntax.

    Do NOT touch anything else in these files — this task is strictly about decorator cleanup.
  </action>
  <verify>pnpm build passes with zero errors</verify>
  <done>Zero instances of `standalone: true` in any @Component or @Pipe decorator. Zero CommonModule imports. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add ChangeDetectionStrategy.OnPush to every @Component</name>
  <files>
    Every .component.ts file in src/app/ (approximately 25-30 components).
    Run: grep -rL "ChangeDetectionStrategy" src/app --include="*.component.ts" to find all components missing it.
  </files>
  <action>
    Add `changeDetection: ChangeDetectionStrategy.OnPush` to every @Component decorator that doesn't already have it.

    Add the import: `import { ChangeDetectionStrategy } from '@angular/core';` — merge with existing @angular/core import.

    Do NOT change anything else in the files — this is strictly adding OnPush.

    The app uses `provideZonelessChangeDetection()` so this won't break anything at runtime, but ANGULAR-GUIDE.md requires it on every component.
  </action>
  <verify>
    grep -rL "ChangeDetectionStrategy" src/app --include="*.component.ts" returns zero results.
    pnpm build passes with zero errors.
  </verify>
  <done>Every @Component decorator in the project has `changeDetection: ChangeDetectionStrategy.OnPush`. Build succeeds.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] `grep -r "standalone: true" src/app --include="*.ts"` returns zero results
- [ ] `grep -r "CommonModule" src/app --include="*.ts"` returns zero results
- [ ] `grep -rL "ChangeDetectionStrategy" src/app --include="*.component.ts"` returns zero results
</verification>

<success_criteria>
- Zero `standalone: true` in any decorator
- Zero `CommonModule` imports
- All components have ChangeDetectionStrategy.OnPush
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-standards/02-01-SUMMARY.md`
</output>
