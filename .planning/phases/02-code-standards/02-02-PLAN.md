---
phase: 02-code-standards
plan: 02
type: execute
---

<objective>
Replace all constructor injection with `inject()` and replace `@HostListener` decorators with the `host` object in @Component metadata.

Purpose: ANGULAR-GUIDE.md mandates inject() over constructor injection and host object over @HostListener/@HostBinding. These are consistent patterns that every file must follow.
Output: Zero constructor-injected dependencies, zero @HostListener decorators.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@ANGULAR-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace constructor injection with inject() in all files</name>
  <files>
    src/app/core/auth/auth.service.ts (constructor(private router: Router)),
    src/app/next-step-word-study/next-step-word-study.component.ts (constructor(private route, private wordService)),
    src/app/practice/math/quiz/quiz.component.ts (constructor(private sanitizer: DomSanitizer)),
    src/app/practice/math/quiz2/quiz2.component.ts (constructor(private sanitizer: DomSanitizer)),
    src/app/practice/math/quiz3/quiz3.component.ts (constructor(private sanitizer: DomSanitizer)),
    src/app/word.service.ts (empty constructor — just delete it)
  </files>
  <action>
    For each file with constructor injection:
    1. Add `inject` to the @angular/core import (if not already present)
    2. Move the dependency to a class field: `private readonly foo = inject(FooService);`
    3. Remove the constructor parameter
    4. If the constructor becomes empty, delete it entirely
    5. If the constructor has initialization logic (like QuizComponent calling this.generateProblem()), keep the constructor but remove only the parameters

    Pattern:
    ```
    // BEFORE
    constructor(private sanitizer: DomSanitizer) {
      this.generateProblem('addition');
    }

    // AFTER
    private readonly sanitizer = inject(DomSanitizer);
    constructor() {
      this.generateProblem('addition');
    }
    ```

    For WordService: delete the empty `constructor() { }` entirely.

    Also search for any other constructor injection missed in the initial audit:
    `grep -rn "constructor(private" src/app --include="*.ts"`
  </action>
  <verify>
    grep -rn "constructor(private" src/app --include="*.ts" returns zero results.
    pnpm build passes.
  </verify>
  <done>Zero constructor-injected dependencies in any file. All dependencies use inject(). Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace @HostListener with host object in navbar component</name>
  <files>
    src/app/shared/layout/navbar.component.ts
  </files>
  <action>
    The navbar component has two @HostListener decorators (for window:scroll and document:click). Replace them with the `host` property in the @Component decorator.

    Pattern:
    ```
    // BEFORE
    @HostListener('window:scroll') onScroll() { ... }
    @HostListener('document:click', ['$event']) onDocClick(e: Event) { ... }

    // AFTER (in @Component decorator)
    host: {
      '(window:scroll)': 'onScroll()',
      '(document:click)': 'onDocClick($event)',
    }
    ```

    Remove the @HostListener import from @angular/core.

    Also search for any other @HostListener or @HostBinding usage:
    `grep -rn "@HostListener\|@HostBinding" src/app --include="*.ts"`
  </action>
  <verify>
    grep -rn "@HostListener\|@HostBinding" src/app --include="*.ts" returns zero results.
    pnpm build passes.
  </verify>
  <done>Zero @HostListener or @HostBinding decorators. All host bindings use the host object. Build passes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm build` succeeds without errors
- [ ] `grep -rn "constructor(private" src/app --include="*.ts"` returns zero results
- [ ] `grep -rn "@HostListener\|@HostBinding" src/app --include="*.ts"` returns zero results
</verification>

<success_criteria>
- Zero constructor injection
- Zero @HostListener/@HostBinding decorators
- All dependencies use inject()
- All host bindings use host object
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-standards/02-02-SUMMARY.md`
</output>
