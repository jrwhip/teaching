---
phase: 03-grid-quizzes
type: execute
---

<objective>
Restyle quiz2 grid-based quiz component with design tokens.

Purpose: Remove per-component color picker, replace hardcoded colors with CSS custom properties, remove duplicated .frac CSS (now global), wire MathResultsService.
Output: quiz2 renders in dark mode, grid reflows on mobile, zoom modal looks correct, results in DynamoDB.
</objective>

<context>
@.planning/BRIEF.md
@src/app/practice/math/shared/math-results.service.ts
@src/app/practice/math/shared/problem-taxonomy.ts
@src/styles/_tokens.scss
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove color picker and hardcoded styles from quiz2</name>
  <files>src/app/practice/math/quiz2/quiz2.component.ts, src/app/practice/math/quiz2/quiz2.component.html, src/app/practice/math/quiz2/quiz2.component.scss</files>
  <action>Remove colorTheme signal, colorOptions array, setColor() method, color picker select element, [style.background-color] binding. Replace .quiz2-page wrapper with .section > .container. Replace .header-bar with .card-flat using token backgrounds. Replace .problem-card backgrounds with var(--bg-surface). Remove duplicated .frac CSS blocks (already global from Phase 1). Replace .zoom-modal background with var(--bg-surface). Scoreboard uses token-based styling.</action>
  <verify>pnpm build passes</verify>
  <done>No colorTheme/colorOptions/setColor, no hardcoded colors, no .frac duplicates</done>
</task>

<task type="auto">
  <name>Task 2: Wire MathResultsService into quiz2</name>
  <files>src/app/practice/math/quiz2/quiz2.component.ts</files>
  <action>Inject MathResultsService. Call startNewSession() in generateAllProblems(). Call recordAttempt() in checkAnswer() with taxonomy lookup from QUIZ2_INDEX_TYPES.</action>
  <verify>pnpm build passes</verify>
  <done>Results recorded via MathResultsService with correct problem type taxonomy</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Dark mode works
- [ ] Grid reflows on mobile
- [ ] Zoom modal renders with token colors
- [ ] No colorTheme/colorOptions/setColor artifacts remain
</verification>

<success_criteria>
- Color picker fully removed
- All colors from design tokens
- .frac CSS not duplicated
- MathResultsService wired with taxonomy
</success_criteria>
