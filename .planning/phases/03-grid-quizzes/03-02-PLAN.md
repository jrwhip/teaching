---
phase: 03-grid-quizzes
type: execute
---

<objective>
Restyle quiz3 grid-based quiz component with design tokens.

Purpose: Identical treatment to quiz2 (near-duplicate component). Remove color picker, token replacement, global fractions, MathResultsService wiring.
Output: quiz3 renders in dark mode, grid reflows on mobile, results in DynamoDB.
</objective>

<context>
@.planning/BRIEF.md
@src/app/practice/math/shared/math-results.service.ts
@src/app/practice/math/shared/problem-taxonomy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove color picker and restyle quiz3</name>
  <files>src/app/practice/math/quiz3/quiz3.component.ts, src/app/practice/math/quiz3/quiz3.component.html, src/app/practice/math/quiz3/quiz3.component.scss</files>
  <action>Same treatment as quiz2: remove colorTheme/colorOptions/setColor, replace hardcoded colors with tokens, remove duplicate .frac blocks, token-based zoom modal. Add questionText to Problem interface for taxonomy display. Full SCSS rewrite with design tokens.</action>
  <verify>pnpm build passes</verify>
  <done>No color picker artifacts, all token-based colors, no .frac duplicates</done>
</task>

<task type="auto">
  <name>Task 2: Wire MathResultsService into quiz3</name>
  <files>src/app/practice/math/quiz3/quiz3.component.ts</files>
  <action>Inject MathResultsService. Call startNewSession() in generateAllProblems(). Call recordAttempt() in checkAnswer() with taxonomy lookup from QUIZ3_INDEX_TYPES. Add questionText to all 10 problem generators.</action>
  <verify>pnpm build passes</verify>
  <done>Results recorded with correct QUIZ3_INDEX_TYPES taxonomy for all 10 problem types</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Dark mode works
- [ ] Grid reflows on mobile
- [ ] All 10 generators include questionText
- [ ] No colorTheme/colorOptions/setColor artifacts remain
</verification>

<success_criteria>
- Identical quality to quiz2 restyle
- All 10 problem types tracked with correct taxonomy
- No hardcoded colors or duplicate CSS
</success_criteria>
