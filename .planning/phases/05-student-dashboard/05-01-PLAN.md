---
phase: 05-student-dashboard
type: execute
---

<objective>
Add query methods to MathResultsService and enhance math-menu with student stats display.

Purpose: Students should see their progress — total problems solved, accuracy, best streak, and recent activity — directly on the math practice menu page.
Output: MathResultsService with query/computed signals, math-menu showing stats when authenticated.
</objective>

<context>
@.planning/BRIEF.md
@src/app/practice/math/shared/math-results.service.ts
@src/app/practice/math/math-menu.component.ts
@src/app/core/auth/auth.service.ts
@amplify/data/resource.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add query methods and computed signals to MathResultsService</name>
  <files>src/app/practice/math/shared/math-results.service.ts</files>
  <action>Add exported interfaces: PerformanceCounterRecord, RecentAttemptRecord. Add signals: performanceCounters, recentAttempts, statsLoading. Add computed signals: totalCorrect, totalIncorrect, totalAccuracy, bestStreak. Add methods: loadStudentStats(studentId?) fetches counters and recent attempts, loadPerformanceCounters(studentId) queries listPerformanceCounterByStudentId, loadRecentAttempts(studentId, limit=10) queries listProblemAttemptByStudentIdAndAttemptedAt with sortDirection DESC.</action>
  <verify>pnpm build passes</verify>
  <done>Query methods return data from DynamoDB, computed signals aggregate correctly</done>
</task>

<task type="auto">
  <name>Task 2: Enhance math-menu with stats display</name>
  <files>src/app/practice/math/math-menu.component.ts, src/app/practice/math/math-menu.component.html, src/app/practice/math/math-menu.component.scss</files>
  <action>Refactor to external template/style files. Add AuthService and MathResultsService injection. On init when authenticated, call results.loadStudentStats(). Stats row: 4 stat cards (Problems Solved, Accuracy, Best Streak, Correct) with loading state. Activity cards grid unchanged. Recent activity section: last 10 attempts with checkmark/x icons, type labels, answers, dates. Graceful fallback when not authenticated (cards without stats). Add formatType() and formatDate() helpers.</action>
  <verify>pnpm build passes</verify>
  <done>Stats display correctly for authenticated student, menu works when not authenticated</done>
</task>

</tasks>

<verification>
- [ ] pnpm build passes
- [ ] Stats row shows correct aggregates when authenticated
- [ ] Recent activity lists last 10 attempts
- [ ] Menu still works when not authenticated (no stats, no errors)
- [ ] Loading states display while fetching
</verification>

<success_criteria>
- MathResultsService has full query capability
- math-menu shows personalized student stats
- Graceful degradation when unauthenticated
</success_criteria>
