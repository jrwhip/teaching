---
phase: 01-angular-upgrade
type: execute
---

<objective>
Modernize tooling: ESLint 8 → 9 flat config, Jest → Vitest, ag-Grid 30 → 35.

Purpose: ESLint 8 is EOL, Jest is deprecated in Angular 21 (Vitest is the recommended runner), and ag-Grid 30 is 5 major versions behind. Clean up the tooling stack to match the upgraded framework.
Output: All three tools upgraded and working.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@.planning/phases/01-angular-upgrade/01-01-SUMMARY.md
@.planning/phases/01-angular-upgrade/01-02-SUMMARY.md
@package.json
@.eslintrc.json
@jest.config.ts
@jims-app/jest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ESLint 8 → 9 flat config</name>
  <files>.eslintrc.json, eslint.config.js, package.json</files>
  <action>
    1. Create `eslint.config.js` (flat config format) replacing `.eslintrc.json`
    2. Migrate all rules from .eslintrc.json to flat config syntax
    3. Use @angular-eslint/eslint-plugin and @typescript-eslint/eslint-plugin compatible with ESLint 9
    4. Update package.json: eslint to ^9.x, update @angular-eslint/* and @typescript-eslint/* to ESLint 9 compatible versions
    5. Delete .eslintrc.json
    6. Run `pnpm install`

    Use the Nx ESLint plugin's flat config support if available in Nx 22.3.
  </action>
  <verify>`pnpm lint` passes with zero errors using the new flat config</verify>
  <done>eslint.config.js exists. .eslintrc.json deleted. ESLint 9.x in package.json. Lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Jest → Vitest</name>
  <files>package.json, jest.config.ts, jims-app/jest.config.ts, vitest.config.ts, jims-app/src/**/*.spec.ts</files>
  <action>
    1. Add vitest, @analogjs/vitest-angular (or @nx/vite vitest support) to devDependencies
    2. Create vitest.config.ts (or vite.config.ts with test block) at workspace root
    3. Configure for Angular component testing (needs analog or similar transform)
    4. Remove jest, jest-preset-angular, ts-jest, @types/jest from package.json
    5. Delete jest.config.ts files
    6. Update any `describe`/`it`/`expect` imports if needed (Vitest uses same API, may need import changes)
    7. Update Nx project.json test target to use vitest executor
    8. Run `pnpm install`

    If @nx/vite has built-in Angular Vitest support in 22.3, use that. Otherwise use @analogjs/vitest-angular.
  </action>
  <verify>`pnpm test` runs all spec files and passes</verify>
  <done>Vitest runs all tests. Jest fully removed from dependencies. No jest.config files remain.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade ag-Grid 30 → 35</name>
  <files>package.json, jims-app/src/**/*grid*/**/*.ts, jims-app/src/**/*grid*/**/*.html</files>
  <action>
    1. Update ag-grid-community and ag-grid-angular to ^35.x in package.json
    2. Run `pnpm install`
    3. Fix any breaking API changes between ag-Grid 30 and 35:
       - Module registration API changed (ag-Grid 31+ uses modular approach)
       - Check ColDef property changes
       - Check theme/styling changes (ag-Grid 32+ has new theming)
       - Update imports if package structure changed
    4. Verify grid renders with existing data

    ag-Grid 30→35 is a significant jump. Check the ag-Grid changelog for each major version's breaking changes.
  </action>
  <verify>`pnpm build` succeeds. Grid component renders data correctly.</verify>
  <done>ag-Grid 35.x in package.json. Grid renders. No console errors from ag-Grid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migrated ESLint to flat config, Jest to Vitest, ag-Grid to 35</what-built>
  <how-to-verify>
    1. Run: `pnpm lint` — should pass
    2. Run: `pnpm test` — should pass with Vitest
    3. Run: `pnpm start`
    4. Navigate to any view with ag-Grid
    5. Confirm: grid renders, sorting works, filtering works, data displays correctly
    6. Confirm: no console errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint` passes with ESLint 9 flat config
- [ ] `pnpm test` passes with Vitest
- [ ] `pnpm build` succeeds
- [ ] ag-Grid 35 renders correctly
- [ ] No jest or eslintrc artifacts remain
</verification>

<success_criteria>
- ESLint 9 with flat config, zero lint errors
- Vitest running all tests, Jest fully removed
- ag-Grid 35 rendering correctly
- All three tools working together
- Human verified via dev server
</success_criteria>

<output>
After completion, create `.planning/phases/01-angular-upgrade/01-03-SUMMARY.md`
</output>
