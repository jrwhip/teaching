---
phase: 01-angular-upgrade
type: execute
---

<objective>
Migrate Nx 18.0.7 → 22.3+ and Angular 17.2.4 → 21.x with TypeScript 5.3 → 5.8+.

Purpose: The app is 4 major versions behind. Angular 17 is end-of-life. This is foundation work — upgrade the platform before building anything else on it.
Output: App compiles and runs on Angular 21 / Nx 22.3 / TypeScript 5.8+ with zero regressions.
</objective>

<execution_context>
@~/.claude/skills/kitt-create-plans/workflows/execute-phase.md
@~/.claude/skills/kitt-create-plans/templates/summary.md
</execution_context>

<context>
@.planning/BRIEF.md
@.planning/ROADMAP.md
@package.json
@nx.json
@tsconfig.base.json
@jims-app/tsconfig.json
@jims-app/tsconfig.app.json
@jims-app/src/main.ts
@jims-app/src/app/app.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Nx migrate to latest</name>
  <files>package.json, nx.json, migrations.json</files>
  <action>
    Run `pnpm nx migrate latest` to generate migrations.json and update package.json.

    If the single jump fails (Nx 18 → 22 is a big gap), step through major versions:
    1. `pnpm nx migrate @nx/workspace@19` → `pnpm install` → `pnpm nx migrate --run-migrations`
    2. `pnpm nx migrate @nx/workspace@20` → `pnpm install` → `pnpm nx migrate --run-migrations`
    3. `pnpm nx migrate @nx/workspace@21` → `pnpm install` → `pnpm nx migrate --run-migrations`
    4. `pnpm nx migrate @nx/workspace@22` → `pnpm install` → `pnpm nx migrate --run-migrations`

    Delete migrations.json after each step completes.

    CRITICAL: Use pnpm only. Never npm. Never npx. Use `pnpm nx` not `npx nx`.
  </action>
  <verify>`pnpm nx report` shows @nx/workspace 22.x and @angular/core 21.x</verify>
  <done>package.json shows Angular 21.x, Nx 22.x, TypeScript 5.8+. No migrations.json remaining.</done>
</task>

<task type="auto">
  <name>Task 2: Fix compilation errors from breaking changes</name>
  <files>jims-app/src/**/*.ts, jims-app/src/**/*.html</files>
  <action>
    Run `pnpm build` and fix all compilation errors.

    Known breaking changes across Angular 17→21:
    - Router: withComponentInputBinding() API changes
    - HttpClient: standalone imports may have changed
    - Signals: computed/effect API refinements
    - Template syntax: @if/@for are stable but check for edge cases
    - TypeScript 5.8 stricter checks may surface new type errors

    Fix each error, keeping changes minimal. Don't refactor — just fix what breaks.
  </action>
  <verify>`pnpm build` completes with zero errors</verify>
  <done>Production build succeeds. No TypeScript errors. No Angular compiler errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Migrated app from Angular 17/Nx 18 to Angular 21/Nx 22.3</what-built>
  <how-to-verify>
    1. Run: `pnpm start`
    2. Visit: http://localhost:4200
    3. Navigate: all routes (/, /next-step-word-study, /math, math sub-routes)
    4. Test: dark mode toggle
    5. Test: login/signup flow
    6. Confirm: all math components render, ag-Grid displays data, no console errors
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm nx report` shows Angular 21.x, Nx 22.x, TypeScript 5.8+
- [ ] `pnpm build` succeeds with zero errors
- [ ] `pnpm start` serves the app without errors
- [ ] All routes render correctly
- [ ] Dark mode works
- [ ] No console errors
</verification>

<success_criteria>
- Angular 21.x running with Nx 22.3+
- TypeScript 5.8+ compiling all source
- Zero build errors
- All existing functionality preserved
- Human verified via dev server
</success_criteria>

<output>
After completion, create `.planning/phases/01-angular-upgrade/01-01-SUMMARY.md`
</output>
